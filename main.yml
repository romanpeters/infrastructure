---
- name: Configure Base
  hosts: default
  strategy: "{{ strategy | default('ansible.builtin.linear') }}"
  become: true
  roles:
    - role: base

- name: Configure Docker
  hosts: docker
  become: true
  roles:
    - role: docker
